name: AWS Code Deploy

on: 
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install dependencies
        run: |
          npm ci

      - name: Build React App
        run: |
          npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: AKIATWE4RYDUEA5S4YAK
          aws-secret-access-key: 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
          aws-region: us-east-1 

      - name: Deploy to AWS Elastic Beanstalk
        run: |
          npm install -g awsebcli
          eb deploy YourElasticBeanstalkEnvironmentName  # Replace with your Elastic Beanstalk environment name

# Define your secrets in the repository settings:
# AWS_ACCESS_KEY_ID: YourAWSAccessKey
# AWS_SECRET_ACCESS_KEY: YourAWSSecretKeyname: AWS Code Deploy

on: 
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install dependencies
        run: |
          npm ci

      - name: Build React App
        run: |
          npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: AKIATWE4RYDUEA5S4YAK
          aws-secret-access-key: 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
          aws-region: us-east-1 

      - name: Deploy to AWS Elastic Beanstalk
        run: |
          npm install -g awsebcli
          eb deploy YourElasticBeanstalkEnvironmentName  # Replace with your Elastic Beanstalk environment name

# Define your secrets in the repository settings:
# AWS_ACCESS_KEY_ID: YourAWSAccessKey
# AWS_SECRET_ACCESS_KEY: YourAWSSecretKey
